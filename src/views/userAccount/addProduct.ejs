<!DOCTYPE html>
<html>
<head>
    <title>Add Product</title>
    <link rel="stylesheet" type="text/css" href="/css/productManagenent.css">
    <link rel="stylesheet" type="text/css" href="/css/defaults.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Lato:ital,wght@0,100;0,300;0,400;0,700;0,900;1,100;1,300;1,400;1,700;1,900&family=Rubik+Doodle+Shadow&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
    <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@24,400,0,0" />
</head>
<body>
    <div class="header">
        <a href="/"><div class="logo">FajrantInator.pl</div></a>
        
        <div class="cart-div">
            <a href="<%= (accountType === 'guest') ? '/login' : '/logout' %> "><div class="button primary"><%= (accountType === 'guest') ? 'Zaloguj się' : 'Wyloguj się' %></div></a>
        </div>
    </div>
    <div class="container">
        <h1> <a href="/account" class="a-center"><span class="material-symbols-outlined">arrow_back_ios</span></a> Dodaj produkt:</h1>
    
    <form action="/account/addProduct" method="POST" enctype="multipart/form-data" class="inputsForm">
        <% if (error) { %>
            <div style="color: red;"><%= error %></div>
        <% } %>
        <% if (success) { %>
            <div style="color: green;"><%= success %></div>   
        <% } %>

        <label>
            Nazwa pdoruktu:
            <input type="text" name="name" required>
        </label>

        <label>
            Miniaturka (max 5MB):
            <input type="file" name="file">
        </label>
        
        <label>
            Nazwa przedmiotu:
            <input type="text" name="course" required>
        </label>
        

        <label>
            Numer listy:
            <input type="number" name="listNumber" required>
        </label>

        <label>
            Numer zadania:
            <input type="number" name="taskNumber" required>
        </label>
        
        <label>
            Cena:
            <input type="number" name="price" required>
        </label>

        <label>
            Opis:
            <textarea name="description" required></textarea>
        </label>
        
        <label>
            Plik z rozwiązaniem (max 5MB):
            <input type="file" name="file">
        </label>
        
        <!-- TODO syntax highlighing, additional options for code testing? -->
        <label>
            Rozwiązanie w formie kodu:
            <textarea name="solutionCode" required></textarea>
        </label>
        
        <input type="submit" class="button primary" value="Dodaj produkt">
    </form>
</body>
</html>
